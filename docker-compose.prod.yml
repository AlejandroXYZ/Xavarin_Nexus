services:
  proxy:
    image: nginx:stable-alpine
    restart: "always"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./proxy_data/:/data
      - ./letsencrypt/:/etc/letsencrypt
    networks:
      - main-network
    depends_on:
      - backend

networks:
  main-network:
